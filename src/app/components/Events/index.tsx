import * as React from 'react';

import './index.scss';

const Events = (): JSX.Element => {
    return (
        <div className="Events">
            <h1 className="Events-title">I'm about page</h1>
        </div>
    );
};

export default Events;
